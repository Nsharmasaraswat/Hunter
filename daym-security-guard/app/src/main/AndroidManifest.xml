<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gtpautomation.securityguard">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecurityGuard"
        android:usesCleartextTraffic="true">
        <activity android:name=".pages.add_driver.AddDriverActivity">
<!--             <intent-filter>-->
<!--             <action android:name="android.intent.action.MAIN" />-->
<!--             <category android:name="android.intent.category.LAUNCHER" />-->
<!--             </intent-filter>-->
        </activity>
        <activity android:name=".pages.add_truck.AddTruckActivity">


        </activity>
        <activity
            android:name=".pages.search_truck.SearchTruckActivity"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity
            android:name=".pages.all_items.AllItemsActivity"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity
            android:name=".pages.QRCodeActivity"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity
            android:name=".pages.ScanActivity"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity
            android:name=".pages.AppointmentDetailsActivity"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity android:name=".utils.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pages.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.SecurityGuard.NoActionBar" />
        <activity
            android:name=".pages.LoginActivity"
            android:theme="@style/Theme.SecurityGuardNoActionBar" />
    </application>

</manifest>